<?xml version="1.0" encoding="UTF-8"?>
<clientConfig version="1.1">
  <emailProvider id="polyhedra.nl">
    <domain>polyhedra.nl</domain>

    <displayName>Polyhedra Mail (iCloud)</displayName>
    <displayShortName>Polyhedra</displayShortName>

    <!-- Incoming: iCloud IMAP -->
    <incomingServer type="imap">
      <hostname>imap.mail.me.com</hostname>
      <port>993</port>
      <socketType>SSL</socketType>
      <!-- iCloud: usually *name* of the iCloud address, not full address -->
      <authentication>password-cleartext</authentication>
      <username>%EMAILLOCALPART%</username>
    </incomingServer>

    <!-- Outgoing: iCloud SMTP -->
    <outgoingServer type="smtp">
      <hostname>smtp.mail.me.com</hostname>
      <port>465</port>
      <socketType>SSL</socketType>
      <authentication>password-cleartext</authentication>
      <!-- iCloud wants full iCloud Mail address for SMTP -->
      <username>%EMAILLOCALPART%@icloud.com</username>
    </outgoingServer>

    <!-- Use whatever address the user typed as visible identity -->
    <identity>
      <emailAddress>%EMAILADDRESS%</emailAddress>
    </identity>
  </emailProvider>
</clientConfig>
